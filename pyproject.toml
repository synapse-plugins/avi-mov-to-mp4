[project]
name = "avi-mov-to-mp4"
version = "2.1.4"
description = "avi-mov-to-mp4 plugin"
requires-python = ">=3.10"
dependencies = [
    "synapse-sdk",
    "ffmpeg-python",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
    "ruff>=0.8",
]
